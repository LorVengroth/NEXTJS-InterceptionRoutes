export default function LoginPage(){
    return(
        <div>
            <h1>This is how we use routes group the URL didnt show the (auth) folder </h1>
            <h2>This is Login Page </h2>
        </div>
    )
}